#include<stdio.h>

int main(){
  char x;

  printf("Enter anything of single character : ");
  scanf("%c", &x);

  (x >= '0' && x <= '9' &&  printf("%c is a digit\n", x)) || ((x >= 'a' && x <= 'z' || x >= 'A' && x <= 'Z') && printf("%c is not a digit\n", x));

  return 0;
}
